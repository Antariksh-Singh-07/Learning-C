#include <stdio.h>

int main()
{

    // arithmetic operators

    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (modulus)
    // ++ increment
    // -- decrement

    int c = 5;
    int b = 2;

    // int a = c + b; // 7
    // int a = c - b; // 3
    // int a = c * b; // 10
    // float a = c / b; // OT: 2.000000 because denominator is int
    // float a = c / (float) b; // conversion of a variable can be done by preceeding it with the desired data type
    // int a = c % b; // 1
    // c++; // c+1
    // c--; // c-1

    // augumented assignment operators

    // x = x + 2 == x+=2

    // x = x - 3 == x-=3

    // x = x * 4 == x*=4

    // x = x / 5 == x/=5

    // x = x % 2 == x%=2

    return 0;
}